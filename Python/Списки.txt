spisok=[]
Кортеж cortege=tuple()
l=[1, 2, ['rarara'], 3]

ГЕНЕРАТОРЫ
c=[c*4 for c in range(30)]
c=[с*15 for c in range(30) if c%3==0]
c = [c + d for c in 'list' if c != 'i' for d in 'spam' if d != 'a']

x in s - есть ли элемент в списке (bool)
x not in s

Функции:
min(s)
max(s)
list.append(x)  добавляет x  в конец списка
list.extend(leest) - добавляет список leest  в конец списка
list.insert(i, x) - вставляет на i-й элемент х, последующие смещает вперед
list.remove(x) - удаляет первый попавшийся x, если такого нет - выдает error
list.pop([i]) - удаляет i-й элемент и возвращает его, если индекса нет - возвращает последний и стирает
list.index(x, [start [end]]) возвращает первый же элемент x в промежутке от start до end
list.count(x) - возвращает количество x в списке
list.sort(key=функция) - сортирует список на основе функции (посмотреть плотнее про эту сорт)
list.reverse() - разворачивает список
list.copy(x) - поверхностная копия списка в x
list.clear() - очищает список

